requests
httpx
python-socketio[client]
websocket-client
psutil
python-dotenv
pydantic
tenacity
mss

# Windows-only (installed conditionally by install scripts; harmless to list,
# pip will still try to install these on Linux unless you use the platform
# markers below - keeping them here so `pip install -r requirements.txt`
# works correctly on both OSes without a second requirements file).
pywin32>=306; sys_platform == "win32"
WMI>=1.5.1; sys_platform == "win32"

# Packaging / testing (not needed at runtime, but kept in one file per the
# requested project structure; use `pip install -r requirements.txt` for a
# full dev environment, or `pip install -r requirements.txt --no-deps` +
# manual runtime-only install if you want a leaner deployed footprint).
pyinstaller
pytest
pytest-mock
